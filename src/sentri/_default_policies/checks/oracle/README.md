# Oracle Health Checks

Drop `.md` health check files here for Oracle databases.

Same format as `checks/*.md` — YAML frontmatter (schedule, severity, routes_to) + Markdown sections (Health Query, Threshold, Recommended Action).

Existing checks in the parent `checks/` directory also apply to Oracle databases for backwards compatibility.
