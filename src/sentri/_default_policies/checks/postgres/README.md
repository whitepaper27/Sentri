# PostgreSQL Health Checks

Drop `.md` health check files here for PostgreSQL databases.

Same format as `checks/*.md` — YAML frontmatter (schedule, severity, routes_to) + Markdown sections (Health Query, Threshold, Recommended Action).

Example checks: bloat_detection, replication_lag_trend, unused_indexes, long_running_transactions, connection_pool_usage.
