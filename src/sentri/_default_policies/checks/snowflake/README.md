# Snowflake Health Checks

Drop `.md` health check files here for Snowflake warehouses.

Same format as `checks/*.md` — YAML frontmatter (schedule, severity, routes_to) + Markdown sections (Health Query, Threshold, Recommended Action).

Example checks: credit_usage_trend, warehouse_utilization, clustering_health, storage_growth_projection, failed_tasks.
