# SQL Server Health Checks

Drop `.md` health check files here for SQL Server databases.

Same format as `checks/*.md` — YAML frontmatter (schedule, severity, routes_to) + Markdown sections (Health Query, Threshold, Recommended Action).

Example checks: index_fragmentation, tempdb_growth, log_backup_freshness, wait_stats_trend, agent_job_failures.
