# PostgreSQL Verified Syntax Documentation

Ground truth SQL syntax docs for PostgreSQL databases. Same structure as `docs/oracle/` — organized by version and category.

Future structure:
```
docs/postgres/
├── 16/
│   ├── vacuum/
│   ├── replication/
│   └── performance/
├── 15/
│   └── ...
└── rules/
    ├── partitioning.md
    └── connection_limits.md
```
